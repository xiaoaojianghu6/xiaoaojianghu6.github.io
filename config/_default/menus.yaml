# Navigation Links
#   The weight parameter defines the order that the links will appear in.
main:
  - name: Home
    url: /
    weight: 10
    params:
      align: left  # 左侧对齐

  - name: Topics
    url: tags/
    weight: 10
    params:
      align: right  # 右侧对齐

